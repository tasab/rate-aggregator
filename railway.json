{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install"
  },
  "deploy": {
    "startCommand": "npm run db:migrate && npm run db:seed:all && npm run prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 1
  },
  "environments": {
    "production": {
    },
    "development": {
    }
  }
}